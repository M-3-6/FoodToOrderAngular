import { BlurEffectDirective } from './blur-effect.directive';

describe('BlurEffectDirective', () => {
  it('should create an instance', () => {
    const directive = new BlurEffectDirective();
    expect(directive).toBeTruthy();
  });
});
