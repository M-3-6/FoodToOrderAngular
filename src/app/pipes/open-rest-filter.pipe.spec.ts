import { OpenRestFilterPipe } from './open-rest-filter.pipe';

describe('OpenRestFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new OpenRestFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
